--- app/src/main/java/com/echosystem/localshare/ui/screens/Screens.kt
+++ app/src/main/java/com/echosystem/localshare/ui/screens/Screens.kt
@@ -62,4 +62,88 @@
 fun MainScreen(viewModel: EchoViewModel = hiltViewModel()) {
     var showOnboarding by remember { mutableStateOf(true) }
     val webShareViewModel: WebShareViewModel = hiltViewModel()
     val navController = rememberNavController()
 
     var showMenu by remember { mutableStateOf(false) }
+    val incomingPairingRequest by viewModel.incomingPairingRequest.collectAsState()
+
+    incomingPairingRequest?.let { request ->
+        AlertDialog(
+            onDismissRequest = { viewModel.clearIncomingPairing() },
+            title = {
+                Row(verticalAlignment = Alignment.CenterVertically) {
+                    Icon(Icons.Default.Security, "Shield Guard", tint = MaterialTheme.colorScheme.primary, modifier = Modifier.size(28.dp))
+                    Spacer(modifier = Modifier.width(8.dp))
+                    Text("Connection Inquiry")
+                }
+            },
+            text = {
+                Column {
+                    Text("A device is requesting a pairing connection with your node:", style = MaterialTheme.typography.bodyMedium)
+                    Spacer(modifier = Modifier.height(12.dp))
+                    Card(colors = CardDefaults.cardColors(containerColor = MaterialTheme.colorScheme.surfaceVariant), modifier = Modifier.fillMaxWidth()) {
+                        Column(modifier = Modifier.padding(16.dp)) {
+                            Text("Device Name: ${request.deviceName}", fontWeight = FontWeight.Bold, style = MaterialTheme.typography.bodyLarge)
+                            Spacer(modifier = Modifier.height(4.dp))
+                            Text("ID: ${request.deviceId}", style = MaterialTheme.typography.bodySmall, color = MaterialTheme.colorScheme.onSurfaceVariant)
+                        }
+                    }
+                    Spacer(modifier = Modifier.height(8.dp))
+                    Text("How would you like to proceed?", style = MaterialTheme.typography.bodySmall, color = MaterialTheme.colorScheme.onSurfaceVariant)
+                }
+            },
+            dismissButton = {
+                TextButton(onClick = { viewModel.blockDeviceFromPairing(request) }, colors = ButtonDefaults.textButtonColors(contentColor = MaterialTheme.colorScheme.error), modifier = Modifier.testTag("block_device_button")) {
+                    Text("Block")
+                }
+            },
+            confirmButton = {
+                Row(horizontalArrangement = Arrangement.spacedBy(8.dp)) {
+                    TextButton(onClick = { viewModel.rejectPairing(request) }, modifier = Modifier.testTag("reject_pairing_button")) {
+                        Text("Reject")
+                    }
+                    Button(onClick = { viewModel.acceptPairing(request) }, modifier = Modifier.testTag("accept_pairing_button")) {
+                        Text("Accept")
+                    }
+                }
+            },
+            shape = RoundedCornerShape(24.dp)
+        )
+    }
 
     if (showOnboarding) {
